export interface Create {
  propertyAmenity_id: string;
  property_id: string;
  name: string;
  category: string;
  icon: string;
}

export interface UpdateAmenity {
  propertyAmenity_id: string;
  property_id: string;
  name: string;
  category: string;
  icon: string;
}
